{
  "name": "canvexia",
  "version": "0.0.0",
  "private": true,
  "description": "Canvexia monorepo — apps and shared packages",
  "engines": {
    "node": "22.x"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.10.12"
  }
}
