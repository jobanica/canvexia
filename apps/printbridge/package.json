{
  "name": "resceta-printbridge",
  "version": "1.0.0",
  "private": true,
  "description": "Prints Resceta receipts on the till's own printer, with no dialog.",
  "type": "module",
  "bin": {
    "resceta-printbridge": "src/index.mjs"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
