packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 does not run dependency build scripts unless they are approved here.
# Prisma's postinstall is what puts the generated client in node_modules, so the
# app cannot build without it.
onlyBuiltDependencies:
  - "@prisma/client"
  - prisma
